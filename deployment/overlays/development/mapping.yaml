apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: treetracker-denormalization
spec:
  cors:
    origins: '*'
    methods: GET, POST, PATCH, OPTIONS
    headers:
      - Content-Type
      - Authorization
